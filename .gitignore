# Machine-specific config
zsh/zshrc.local
.tmux.conf.local
.gitconfig.local
agents/claude/settings.local.json
bash/.fzf.bash
zsh/.fzf.zsh
.dot-version
# core.hooksPath points here, so any `lefthook install` (in this or any other
# repo) recreates this file locally — never meant to be shared
git/hooks/pre-commit

# Homebrew
Brewfile.lock.json

# Editor
.vim/.netrwhist
.vim/bundle/

# Compiled binaries (architecture-specific, built by profiles/common/install/audioctl)
bin/audioctl
